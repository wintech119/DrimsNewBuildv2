Help me update the DRIMS PostgreSQL database by replacing the existing event table with a new version.

Requirements:

Drop the existing event table safely (use DROP TABLE IF EXISTS event CASCADE;).

Recreate the table exactly as defined below, following the same field names, constraint naming conventions, and index patterns already used across the DRIMS schema.

Ensure all constraints are created with the exact names provided (pk_event, c_event_1, c_event_2, c_event_3, c_event_4a, c_event_4b).

Ensure the identity column uses the same DRIMS style (generated by default as identity).

After creating the table, verify whether DRIMS' global indexing standards require indexes on FK or status fields—if so, create them using the DRIMS naming pattern (idx_<table>_<field>).

New event table definition:

DROP TABLE IF EXISTS event CASCADE;

create table event
(
    event_id integer not null generated by default as identity
        constraint pk_event primary key,

    event_type varchar(16) not null
        constraint c_event_1 check
        (event_type in ('STORM','TORNADO','FLOOD','TSUNAMI','FIRE','EARTHQUAKE','WAR','EPIDEMIC')),

    start_date date not null        
        constraint c_event_2 check (start_date <= CURRENT_DATE),

    event_name varchar(60) not null,

    event_desc varchar(255) not null,

    impact_desc text not null,

    status_code char(1) not null
        constraint c_event_3 check (status_code in ('A','C')),

    closed_date date
        constraint c_event_4a
        check ((status_code = 'A' and closed_date is null)
            or (status_code = 'C' and closed_date is not null)),

    reason_desc varchar(255)
        constraint c_event_4b
        check ((reason_desc is null and closed_date is null)
            or (reason_desc is not null and closed_date is not null)),

    create_by_id varchar(20) not null,
    create_dtime timestamp(0) without time zone not null,
    update_by_id varchar(20) not null,
    update_dtime timestamp(0) without time zone not null,
    version_nbr integer not null
);


After generating the SQL, validate the script to ensure:
– The DRIMS naming conventions are intact
– No additional schema changes are introduced
– Indexes match the DRIMS convention if needed
– No foreign keys are required for this table